- improve LSTM, CNN LSTM
- improve 1D CNN
- metrics, visualization -> tensorboard
- Presentation Power point
- comparison regarding time, accuracy
    - try different optimizer, architectures, loss functions, dynamic learning rate, initialization, different hyperparameters
- transfer learning, few-shot learning
    - balancing the data
- improve data generation
    - try to use the 561 extracted features instead of raw data
    - Validation set
    - 50% overlap off 128 windows?
- Transformer, GRU or SVM